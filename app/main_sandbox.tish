// Standalone sandbox entry — smaller than full LearnApp (no lesson chrome).
import { createRoot } from "@tishlang/lattish"
import { StandalonePage } from "@tishlang/tish-ide-panels"

const root = document.getElementById("app")
createRoot(root).render(
  <div class="ll-app ll-app-sandbox">
    {StandalonePage({})}
  </div>
)
